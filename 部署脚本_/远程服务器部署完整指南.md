# 🚀 综合布线系统 - 远程服务器部署完整指南

## 📋 部署前准备

### 1. 确认服务器信息

**服务器地址：** 192.168.19.58
**用户名：** yroot
**操作系统：** Ubuntu 20.04+

### 2. 确认本地文件

确保本地有以下文件：
- ✅ 部署脚本_/服务器安装.sh
- ✅ 部署脚本_/服务器启动.sh
- ✅ 部署脚本_/1_拷贝文件到远程服务器.ps1
- ✅ backend/ 目录
- ✅ frontend/ 目录

---

## 🎯 完整部署流程

### 阶段1：文件传输（在Windows上执行）

#### 步骤1.1：打开PowerShell

在Windows上打开PowerShell，进入项目目录：

```powershell
cd "d:\TREA\sjzx-zonghebuxian"
```

#### 步骤1.2：执行文件拷贝脚本

```powershell
cd "d:\TREA\sjzx-zonghebuxian\部署脚本_"
.\1_拷贝文件到远程服务器.ps1
```

**脚本会做什么：**
- 检查SSH连接
- 创建远程目录结构
- 拷贝backend目录
- 拷贝frontend目录
- 拷贝部署脚本
- 验证文件传输

**预期时间：** 5-10分钟（取决于网络速度）

**成功标志：** 看到类似输出：
```
[OK] 文件传输完成！
```

---

### 阶段2：环境检查和安装（在远程服务器上执行）

#### 步骤2.1：SSH连接到远程服务器

在Windows PowerShell中执行：

```powershell
ssh yroot@192.168.19.58
```

**首次连接会提示输入密码，输入：** qazwsx123Q929394

#### 步骤2.2：进入部署脚本目录

连接成功后：

```bash
cd ~/sjzx-zonghebuxian/部署脚本_
```

#### 步骤2.3：添加执行权限

```bash
chmod +x 服务器安装.sh 服务器启动.sh
```

#### 步骤2.4：运行环境检查和安装脚本

```bash
./服务器安装.sh
```

**脚本会做什么：**
1. ✅ 检查SSH连接
2. ✅ 检查操作系统信息
3. ✅ 检查Node.js环境
4. ✅ 如果需要，安装Node.js 18
5. ✅ 检查NPM环境
6. ✅ 如果需要，安装NPM
7. ✅ 检查磁盘空间（至少2GB）
8. ✅ 检查端口使用情况（3001和80）

**预期时间：** 10-20分钟（如果需要安装Node.js）

**成功标志：** 看到类似输出：
```
================================================================================
[OK] 环境检查和安装完成！
================================================================================
```

**如果遇到问题：**
- SSH连接失败：检查服务器IP和网络
- Node.js安装失败：检查网络连接和apt源
- 磁盘空间不足：清理磁盘空间

---

### 阶段3：服务启动（在远程服务器上执行）

#### 步骤3.1：运行服务启动脚本

继续在远程服务器上执行：

```bash
./服务器启动.sh
```

**脚本会做什么：**
1. ✅ 检查远程服务器文件
2. ✅ 检查Node.js和NPM环境
3. ✅ 检查端口使用情况
4. ✅ 安装后端依赖（npm install）
5. ✅ 安装前端依赖（npm install）
6. ✅ 构建前端（npm run build）
7. ✅ 启动后端服务（nohup npm start）
8. ✅ 启动前端服务（nohup npm run preview）
9. ✅ 验证服务状态

**预期时间：** 15-30分钟（取决于网络和构建时间）

**成功标志：** 看到类似输出：
```
================================================================================
[OK] 服务启动完成！
================================================================================

访问URL:
  Frontend: http://192.168.19.58:80
  Backend: http://192.168.19.58:3001
```

**如果遇到问题：**
- 依赖安装失败：检查npm配置和网络
- 构建失败：检查代码错误
- 端口被占用：停止占用端口的服务

---

## ✅ 验证部署

### 验证1：检查服务状态

在远程服务器上执行：

```bash
# 查看后端进程
ps aux | grep "npm start" | grep -v grep

# 查看前端进程
ps aux | grep "npm run preview" | grep -v grep

# 查看端口监听
netstat -tuln | grep -E ":(3001|80)"
```

**预期结果：**
```
yroot     12345  0.0  0.5  123456 ?        Ssl   0:00   0:05 node /home/yroot/sjzx-zonghebuxian/backend/node_modules/.bin/npm start
yroot     12346  0.0  0.3  123456 ?        Ssl   0:00   0:03 node /home/yroot/sjzx-zonghebuxian/frontend/node_modules/.bin/npm run preview

tcp        0      0.0.0.0              0.0.0.0:3001            LISTEN
tcp        0      0.0.0.0              0.0.0.0:80              LISTEN
```

### 验证2：访问服务

在浏览器中访问：

**前端：** http://192.168.19.58:80
**后端：** http://192.168.19.58:3001

**预期结果：**
- ✅ 前端页面正常显示
- ✅ 后端API正常响应
- ✅ 功能可以正常使用

### 验证3：查看服务日志

在远程服务器上执行：

```bash
# 查看后端日志
tail -f ~/sjzx-zonghebuxian/backend/backend.log

# 查看前端日志
tail -f ~/sjzx-zonghebuxian/frontend/frontend.log
```

**预期结果：**
```
[INFO] Starting server on port 3001
[INFO] Server listening on port 3001
```

---

## 🔧 服务管理

### 查看服务状态

```bash
# 查看所有相关进程
ps aux | grep -E "npm start|npm run preview" | grep -v grep

# 查看端口占用
netstat -tuln | grep -E ":(3001|80)"
```

### 停止服务

```bash
# 停止后端服务
pkill -f "npm start"

# 停止前端服务
pkill -f "npm run preview"
```

### 重启服务

```bash
# 进入后端目录
cd ~/sjzx-zonghebuxian/backend

# 重启后端
pkill -f "npm start"
nohup npm start > backend.log 2>&1 &

# 进入前端目录
cd ~/sjzx-zonghebuxian/frontend

# 重启前端
pkill -f "npm run preview"
nohup npm run preview > frontend.log 2>&1 &
```

### 查看日志

```bash
# 实时查看后端日志
tail -f ~/sjzx-zonghebuxian/backend/backend.log

# 实时查看前端日志
tail -f ~/sjzx-zonghebuxian/frontend/frontend.log

# 查看最后100行
tail -n 100 ~/sjzx-zonghebuxian/backend/backend.log
```

---

## ❓ 故障排查

### 问题1：SSH连接失败

**错误信息：**
```
Connection refused
Connection timeout
```

**解决方案：**
1. 检查服务器IP是否正确：192.168.19.58
2. 检查服务器SSH服务是否运行：
   ```bash
   sudo systemctl status ssh
   ```
3. 检查网络连接：
   ```bash
   ping 192.168.19.58
   ```
4. 检查防火墙设置：
   ```bash
   sudo ufw status
   ```

### 问题2：文件传输失败

**错误信息：**
```
Permission denied
No space left on device
```

**解决方案：**
1. 检查磁盘空间：
   ```bash
   df -h
   ```
2. 检查目录权限：
   ```bash
   ls -la ~/sjzx-zonghebuxian/
   ```
3. 清理磁盘空间：
   ```bash
   sudo apt-get clean
   sudo apt-get autoremove
   ```

### 问题3：Node.js安装失败

**错误信息：**
```
Failed to fetch https://deb.nodesource.com/setup_18.x
Package nodejs not found
```

**解决方案：**
1. 检查网络连接：
   ```bash
   ping deb.nodesource.com
   ```
2. 更新apt源：
   ```bash
   sudo apt-get update
   ```
3. 手动安装Node.js：
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```

### 问题4：依赖安装失败

**错误信息：**
```
npm ERR! code ECONNREFUSED
npm ERR! network timeout
```

**解决方案：**
1. 检查npm配置：
   ```bash
   npm config get registry
   ```
2. 使用国内npm镜像：
   ```bash
   npm config set registry https://registry.npmmirror.com
   ```
3. 清理npm缓存：
   ```bash
   npm cache clean --force
   ```

### 问题5：服务启动失败

**错误信息：**
```
Error: listen EADDRINUSE
Port 3001 is already in use
```

**解决方案：**
1. 查看端口占用：
   ```bash
   sudo netstat -tuln | grep -E ":(3001|80)"
   ```
2. 停止占用端口的服务：
   ```bash
   sudo lsof -i :3001
   sudo kill -9 <PID>
   ```
3. 修改端口配置（如果需要）

### 问题6：服务无法访问

**错误信息：**
```
This site can't be reached
Connection refused
```

**解决方案：**
1. 检查服务是否运行：
   ```bash
   ps aux | grep -E "npm start|npm run preview"
   ```
2. 检查防火墙：
   ```bash
   sudo ufw status
   sudo ufw allow 3001
   sudo ufw allow 80
   ```
3. 检查服务日志：
   ```bash
   tail -f ~/sjzx-zonghebuxian/backend/backend.log
   tail -f ~/sjzx-zonghebuxian/frontend/frontend.log
   ```

---

## 📊 性能优化

### 优化1：使用npm镜像加速

```bash
# 设置淘宝npm镜像
npm config set registry https://registry.npmmirror.com

# 验证配置
npm config get registry
```

### 优化2：配置Node.js镜像

```bash
# 编辑npm配置
npm config set disturl https://npmmirror.com/dist

# 或使用cnpm
npm install -g cnpm --registry=https://registry.npmmirror.com
```

### 优化3：配置apt镜像（中国用户）

```bash
# 备份原配置
sudo cp /etc/apt/sources.list /etc/apt/sources.list.bak

# 使用清华源
sudo tee /etc/apt/sources.list > /dev/null <<EOF
deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ focal main restricted universe multiverse
deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ focal-updates main restricted universe multiverse
deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ focal-backports main restricted universe multiverse
deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ focal-security main restricted universe multiverse
EOF

# 更新软件包列表
sudo apt-get update
```

---

## 🔒 安全建议

### 1. 使用防火墙

```bash
# 启用防火墙
sudo ufw enable

# 允许SSH
sudo ufw allow 22/tcp

# 允许HTTP
sudo ufw allow 80/tcp

# 允许后端API
sudo ufw allow 3001/tcp

# 查看状态
sudo ufw status
```

### 2. 定期更新系统

```bash
# 更新软件包列表
sudo apt-get update

# 升级已安装的软件包
sudo apt-get upgrade

# 清理不需要的软件包
sudo apt-get autoremove
```

### 3. 定期备份数据

```bash
# 备份数据库（如果使用数据库）
# 备份配置文件
cp ~/sjzx-zonghebuxian/backend/.env ~/sjzx-zonghebuxian/backup/.env.$(date +%Y%m%d)

# 备份日志
cp ~/sjzx-zonghebuxian/backend/backend.log ~/sjzx-zonghebuxian/backup/backend.log.$(date +%Y%m%d)
```

---

## 📞 监控和日志

### 设置日志轮转

```bash
# 创建日志轮转配置
sudo tee /etc/logrotate.d/sjzx-zonghebuxian > /dev/null <<EOF
~/sjzx-zonghebuxian/backend/backend.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    create 0644 yroot yroot
}

~/sjzx-zonghebuxian/frontend/frontend.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    create 0644 yroot yroot
}
EOF

# 测试配置
sudo logrotate -d /etc/logrotate.d/sjzx-zonghebuxian
```

### 监控服务状态

```bash
# 创建监控脚本
cat > ~/check_services.sh <<'EOF'
#!/bin/bash
# 检查后端服务
if ! pgrep -f "npm start" > /dev/null; then
    echo "$(date): Backend service is not running!" >> ~/service_check.log
    # 可以在这里添加重启逻辑
fi

# 检查前端服务
if ! pgrep -f "npm run preview" > /dev/null; then
    echo "$(date): Frontend service is not running!" >> ~/service_check.log
    # 可以在这里添加重启逻辑
fi
EOF

# 添加执行权限
chmod +x ~/check_services.sh

# 添加到crontab（每5分钟检查一次）
(crontab -l 2>/dev/null; echo "*/5 * * * * ~/check_services.sh") | crontab -
```

---

## 🎯 快速参考命令

### Windows PowerShell命令

```powershell
# 连接到远程服务器
ssh yroot@192.168.19.58

# 拷贝文件
cd "d:\TREA\sjzx-zonghebuxian\部署脚本_"
.\1_拷贝文件到远程服务器.ps1
```

### Linux命令

```bash
# SSH连接
ssh yroot@192.168.19.58

# 查看服务状态
ps aux | grep -E "npm start|npm run preview"

# 查看日志
tail -f ~/sjzx-zonghebuxian/backend/backend.log
tail -f ~/sjzx-zonghebuxian/frontend/frontend.log

# 停止服务
pkill -f "npm start"
pkill -f "npm run preview"

# 重启服务
cd ~/sjzx-zonghebuxian/backend && nohup npm start > backend.log 2>&1 &
cd ~/sjzx-zonghebuxian/frontend && nohup npm run preview > frontend.log 2>&1 &

# 查看端口
netstat -tuln | grep -E ":(3001|80)"

# 查看磁盘空间
df -h

# 查看内存使用
free -h

# 查看CPU使用
top -bn1 | grep "Cpu(s)"
```

---

## 📞 需要帮助？

如果遇到问题：

1. **查看部署脚本日志**：脚本会输出详细的执行信息
2. **查看服务日志**：backend.log和frontend.log
3. **检查系统日志**：/var/log/syslog, /var/log/auth.log
4. **联系技术支持**：提供错误信息和日志

---

## ✅ 部署检查清单

部署完成后，确认以下项目：

- [ ] 文件已成功传输到远程服务器
- [ ] Node.js 18+ 已安装
- [ ] NPM 已安装
- [ ] 后端依赖已安装
- [ ] 前端依赖已安装
- [ ] 前端已构建
- [ ] 后端服务正在运行
- [ ] 前端服务正在运行
- [ ] 端口3001正在监听
- [ ] 端口80正在监听
- [ ] 前端可以访问
- [ ] 后端API可以访问
- [ ] 功能测试通过

---

**准备好开始部署了吗？按照上面的步骤一步步操作吧！** 🚀
